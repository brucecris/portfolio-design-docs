The artifact viewer opened from a `DeliverableCard`. Escape closes, arrow keys page.

```jsx
<Lightbox open={!!active} title={active.title} meta={active.client + ' · ' + active.type}
  index={shot} total={active.shots} placeholder={active.type}
  onClose={close} onPrev={() => step(-1)} onNext={() => step(1)} />
```

The only dark surface in the system. Pills are outlined white except Next, which is solid white on ink.
