A flat outlined label for engagement types and disciplines; used in rows under case-study heroes.

```jsx
<Tag>Service Design</Tag>
```

No variants. Always 2px radius, 1px `--border-rule`, never filled, never shadowed. Lay several out in a flex row with `gap: 8px`.
