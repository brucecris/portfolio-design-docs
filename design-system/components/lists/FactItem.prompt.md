Used in the fact grid under a case-study hero.

```jsx
<ul style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(min(170px,100%),1fr))',gap:'clamp(20px,3vw,32px) clamp(24px,4vw,48px)'}}>
  <FactItem label="My role" value="CX Lead / Principal Designer" />
</ul>
```
