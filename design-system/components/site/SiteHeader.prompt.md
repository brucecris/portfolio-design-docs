The site header. Sticky, flat `--surface-header`, baseline-aligned, wraps on narrow screens.

```jsx
<SiteHeader active="Work" />
```

A blended variant exists for dark hero imagery: transparent background, `mixBlendMode: 'difference'`, white ink, inactive items at #9A9A9A.
